<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<title>müzikHan · STÜDYO PANELİ</title>
<link rel="icon" type="image/png" href="simge.png?v=2">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Space+Mono:wght@400;700&family=Syncopate:wght@700&display=swap" rel="stylesheet">
<style>
:root{
  --bg:#040506; --card:#0c0e10; --card2:#0a0c0e; --line:rgba(255,255,255,.07);
  --line2:rgba(255,255,255,.15); --txt:#ececec; --mut:#7d838a; --dim:#454b52;
  --accent:#ffffff; --green:#22c55e; --green2:#16a34a; --gold:#e3b341;
  --bad:#ff5d5d; --blue:#5b8cff;
}
*{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent;}
html,body{background:var(--bg);color:var(--txt);font-family:'Inter',sans-serif;min-height:100vh;}
body{overflow-x:hidden;}
::-webkit-scrollbar{width:8px;height:8px;}::-webkit-scrollbar-thumb{background:#23262a;border-radius:8px;}
.mono{font-family:'Space Mono',monospace;}.disp{font-family:'Syncopate',sans-serif;}
.eyebrow{font-family:'Space Mono',monospace;font-size:.58rem;letter-spacing:3px;color:var(--dim);text-transform:uppercase;}
button{font-family:inherit;cursor:pointer;}input,textarea,select{font-family:inherit;}
a{color:inherit;}
.hidden{display:none!important;}

/* form controls */
.input{width:100%;padding:13px 14px;background:#08090b;border:1px solid var(--line);border-radius:11px;color:var(--txt);font-size:.86rem;transition:.2s;}
.input:focus{outline:none;border-color:var(--line2);box-shadow:0 0 0 3px rgba(255,255,255,.04);}
.input::placeholder{color:#3a3f44;}
textarea.input{resize:vertical;min-height:90px;line-height:1.6;}
select.input{appearance:none;background-image:linear-gradient(45deg,transparent 50%,var(--mut) 50%),linear-gradient(135deg,var(--mut) 50%,transparent 50%);background-position:calc(100% - 18px) 18px,calc(100% - 13px) 18px;background-size:5px 5px;background-repeat:no-repeat;}
.lbl{display:block;font-size:.6rem;letter-spacing:2px;color:var(--mut);margin-bottom:7px;text-transform:uppercase;font-family:'Space Mono',monospace;}
.field{margin-bottom:14px;}
.row2{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
@media(max-width:480px){.row2{grid-template-columns:1fr;}}

.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;padding:13px 18px;border-radius:12px;border:1px solid var(--line);background:rgba(255,255,255,.02);color:var(--txt);font-size:.8rem;font-weight:600;letter-spacing:.3px;transition:.22s;width:100%;}
.btn:hover{border-color:var(--line2);background:rgba(255,255,255,.05);}
.btn-primary{background:var(--accent);color:#000;border-color:var(--accent);font-weight:700;}
.btn-primary:hover{box-shadow:0 0 22px rgba(255,255,255,.22);}
.btn-green{background:var(--green);color:#04210f;border-color:var(--green);font-weight:800;font-size:.95rem;padding:17px;letter-spacing:.5px;}
.btn-green:hover{background:#2bdc6b;box-shadow:0 0 30px rgba(34,197,94,.4);}
.btn-ghost{background:transparent;}
.btn-danger{border-color:rgba(255,93,93,.3);color:#ff8a8a;}
.btn-danger:hover{background:rgba(255,93,93,.08);border-color:var(--bad);}
.btn-sm{padding:9px 12px;font-size:.7rem;border-radius:9px;width:auto;}
.btn:disabled{opacity:.5;cursor:not-allowed;}

.msg{font-size:.72rem;margin-top:10px;min-height:15px;line-height:1.5;font-family:'Space Mono',monospace;}
.msg.err{color:var(--bad);}.msg.ok{color:var(--green);}

/* ============ LOGIN ============ */
#view-login{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:24px;}
.login-box{width:100%;max-width:380px;}
.login-logo{text-align:center;margin-bottom:30px;}
.login-logo .disp{font-size:1.9rem;letter-spacing:5px;text-shadow:0 0 30px rgba(255,255,255,.2);}
.login-logo .sub{margin-top:9px;font-size:.58rem;letter-spacing:5px;color:var(--mut);}
.login-card{background:var(--card);border:1px solid var(--line);border-radius:20px;padding:26px;}
.login-card h3{font-family:'Syncopate';font-size:.85rem;letter-spacing:1px;margin-bottom:18px;text-align:center;}

/* ============ APP SHELL ============ */
#view-app{display:none;min-height:100vh;}
.topbar{position:sticky;top:0;z-index:60;background:rgba(4,5,6,.82);backdrop-filter:blur(14px);border-bottom:1px solid var(--line);}
.topbar-in{max-width:920px;margin:0 auto;padding:12px 16px;display:flex;align-items:center;gap:12px;}
.brand{font-family:'Syncopate';font-size:.85rem;letter-spacing:2px;white-space:nowrap;}
.brand small{display:block;font-family:'Space Mono';font-size:.5rem;letter-spacing:3px;color:var(--dim);margin-top:3px;}
.top-right{margin-left:auto;display:flex;align-items:center;gap:10px;position:relative;}
.icon-btn{position:relative;width:38px;height:38px;border-radius:11px;border:1px solid var(--line);background:rgba(255,255,255,.02);display:flex;align-items:center;justify-content:center;font-size:1rem;transition:.2s;}
.icon-btn:hover{border-color:var(--line2);}
.badge-count{position:absolute;top:-5px;right:-5px;min-width:17px;height:17px;padding:0 4px;border-radius:9px;background:var(--bad);color:#fff;font-size:.6rem;font-weight:700;display:flex;align-items:center;justify-content:center;font-family:'Space Mono';}
.me-btn{display:flex;align-items:center;gap:8px;padding:6px 10px 6px 6px;border-radius:11px;border:1px solid var(--line);background:rgba(255,255,255,.02);transition:.2s;}
.me-btn:hover{border-color:var(--line2);}
.me-av{width:28px;height:28px;border-radius:8px;background:#15181b;display:flex;align-items:center;justify-content:center;font-family:'Syncopate';font-size:.7rem;color:var(--mut);}
.me-name{font-size:.78rem;font-weight:600;max-width:110px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
.role-chip{font-family:'Space Mono';font-size:.5rem;letter-spacing:.5px;color:var(--dim);text-transform:uppercase;}
@media(max-width:560px){.me-name{display:none;}.brand small{display:none;}}

.dropdown{position:absolute;top:48px;right:0;min-width:200px;background:var(--card);border:1px solid var(--line2);border-radius:14px;padding:7px;box-shadow:0 18px 50px rgba(0,0,0,.6);display:none;z-index:80;}
.dropdown.open{display:block;animation:pop .18s ease;}
@keyframes pop{from{opacity:0;transform:translateY(-6px);}to{opacity:1;transform:none;}}
.dd-item{display:flex;align-items:center;gap:10px;padding:11px 12px;border-radius:9px;font-size:.8rem;color:var(--txt);transition:.15s;cursor:pointer;}
.dd-item:hover{background:rgba(255,255,255,.05);}
.dd-item.danger{color:#ff8a8a;}
.dd-sep{height:1px;background:var(--line);margin:5px 8px;}
.notif-list{max-height:340px;overflow:auto;}
.notif{padding:11px 12px;border-radius:9px;font-size:.76rem;line-height:1.5;color:#cfd3d7;border-bottom:1px solid var(--line);}
.notif:last-child{border-bottom:none;}
.notif.unread{background:rgba(91,140,255,.06);}
.notif .t{font-family:'Space Mono';font-size:.58rem;color:var(--dim);margin-top:4px;}
.notif-empty{padding:24px 12px;text-align:center;color:var(--dim);font-size:.76rem;font-family:'Space Mono';}

/* nav */
.nav{position:sticky;top:63px;z-index:55;background:rgba(4,5,6,.82);backdrop-filter:blur(14px);border-bottom:1px solid var(--line);}
.nav-in{max-width:920px;margin:0 auto;padding:8px 16px;display:flex;gap:6px;overflow-x:auto;scrollbar-width:none;}
.nav-in::-webkit-scrollbar{display:none;}
.nav-btn{flex-shrink:0;padding:10px 15px;border-radius:10px;border:1px solid transparent;background:transparent;color:var(--mut);font-size:.75rem;font-weight:600;letter-spacing:.4px;font-family:'Space Mono';transition:.2s;white-space:nowrap;}
.nav-btn:hover{color:var(--txt);}
.nav-btn.active{color:var(--txt);border-color:var(--line);background:rgba(255,255,255,.05);}

.wrap{max-width:920px;margin:0 auto;padding:24px 16px 90px;}
.screen{display:none;}
.screen.active{display:block;animation:fade .35s ease;}
@keyframes fade{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:none;}}
.screen-head{display:flex;align-items:flex-end;justify-content:space-between;gap:12px;margin-bottom:20px;flex-wrap:wrap;}
.screen-head h2{font-family:'Syncopate';font-size:clamp(1rem,3.5vw,1.25rem);letter-spacing:.5px;}
.screen-head .eyebrow{margin-bottom:6px;}

/* item cards (demos/beats/etc) */
.card-item{background:var(--card);border:1px solid var(--line);border-radius:16px;overflow:hidden;margin-bottom:14px;transition:.25s;}
.card-item:hover{border-color:var(--line2);}
.ci-head{padding:15px 16px 12px;}
.ci-title{font-family:'Space Mono';font-weight:700;font-size:1.02rem;letter-spacing:.3px;word-break:break-word;}
.ci-meta{font-size:.68rem;color:var(--mut);margin-top:5px;display:flex;gap:8px;flex-wrap:wrap;align-items:center;}
.ci-tags{display:flex;gap:6px;flex-wrap:wrap;margin-top:10px;}
.tag{font-family:'Space Mono';font-size:.55rem;letter-spacing:.5px;padding:4px 9px;border-radius:7px;background:rgba(255,255,255,.04);border:1px solid var(--line);color:var(--mut);}
.tag.score{color:var(--gold);border-color:rgba(227,179,65,.35);}
.ci-prod{font-family:'Space Mono';font-size:.6rem;color:var(--dim);text-align:right;padding:0 16px 8px;}
.card-item audio{width:calc(100% - 32px);margin:2px 16px 14px;filter:grayscale(1) invert(.92);}
.ci-actions{display:flex;gap:8px;padding:0 16px 15px;flex-wrap:wrap;align-items:center;}
.linklike{background:none;border:none;color:var(--mut);font-size:.7rem;font-family:'Space Mono';letter-spacing:.5px;padding:6px 2px;}
.linklike:hover{color:var(--txt);}
.linklike.del:hover{color:var(--bad);}
.dl{display:inline-flex;align-items:center;gap:5px;font-size:.68rem;color:var(--blue);font-family:'Space Mono';text-decoration:none;padding:6px 0;}

/* rating drawer */
.drawer{border-top:1px solid var(--line);padding:16px;display:none;background:rgba(255,255,255,.012);}
.drawer.open{display:block;}
.rate-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:12px;}
@media(max-width:480px){.rate-grid{grid-template-columns:1fr;}}
.rate-cell .k{font-family:'Space Mono';font-size:.62rem;letter-spacing:1px;color:var(--mut);margin-bottom:7px;}
.stars{display:flex;gap:5px;}
.star{font-size:1.35rem;color:#2a2e33;cursor:pointer;transition:.12s;line-height:1;}
.star:hover,.star.on{color:var(--gold);transform:scale(1.05);}
.summary-stars{display:inline-flex;gap:2px;}
.summary-stars .s{color:#2a2e33;font-size:.85rem;}.summary-stars .s.on{color:var(--gold);}

/* comments under demo (notes are private ratings; we also keep a simple notes thread off) */
.empty-state{text-align:center;padding:48px 20px;color:var(--dim);}
.empty-state .ic{font-size:1.9rem;opacity:.4;}
.empty-state .t{font-family:'Space Mono';font-size:.78rem;margin-top:12px;letter-spacing:.5px;}
.center-load{text-align:center;padding:50px 0;}
.spinner{width:18px;height:18px;border:2px solid rgba(255,255,255,.2);border-top-color:#fff;border-radius:50%;animation:spin .7s linear infinite;display:inline-block;}
@keyframes spin{to{transform:rotate(360deg);}}

/* admin rows */
.admin-row{display:flex;align-items:center;gap:12px;background:var(--card);border:1px solid var(--line);border-radius:13px;padding:12px 14px;margin-bottom:10px;flex-wrap:wrap;}
.admin-row .grow{flex:1;min-width:140px;}
.admin-row .t{font-family:'Space Mono';font-size:.82rem;word-break:break-word;}
.admin-row .s{font-size:.64rem;color:var(--mut);margin-top:3px;word-break:break-all;}
.role-pill{font-family:'Space Mono';font-size:.55rem;letter-spacing:1px;padding:3px 8px;border-radius:20px;border:1px solid;text-transform:uppercase;}
.role-admin{color:var(--gold);border-color:rgba(227,179,65,.4);}
.role-team{color:var(--blue);border-color:rgba(91,140,255,.4);}
.role-artist{color:var(--green);border-color:rgba(34,197,94,.4);}
.role-producer{color:#c98bff;border-color:rgba(201,139,255,.4);}

/* modal */
.modal-bg{position:fixed;inset:0;z-index:200;background:rgba(0,0,0,.82);backdrop-filter:blur(10px);display:none;align-items:flex-start;justify-content:center;padding:24px 16px;overflow:auto;}
.modal-bg.open{display:flex;}
.modal{width:100%;max-width:480px;background:var(--card);border:1px solid var(--line2);border-radius:20px;padding:24px;margin:auto;}
.modal h3{font-family:'Syncopate';font-size:.92rem;letter-spacing:1px;margin-bottom:18px;}
.filedrop{border:1px dashed var(--line2);border-radius:13px;padding:20px;text-align:center;cursor:pointer;transition:.2s;}
.filedrop:hover{border-color:#fff;background:rgba(255,255,255,.02);}
.filedrop .ic{font-size:1.5rem;}
.filedrop .t{font-family:'Space Mono';font-size:.72rem;margin-top:8px;color:var(--mut);word-break:break-word;}
.filedrop.has{border-style:solid;border-color:var(--green);}.filedrop.has .t{color:var(--green);}
.modal-actions{display:flex;gap:10px;margin-top:18px;}
.progress{height:5px;border-radius:6px;background:#141618;overflow:hidden;margin-top:14px;display:none;}
.progress.show{display:block;}.progress>i{display:block;height:100%;width:0;background:var(--green);transition:width .15s;}
.check-row{display:flex;align-items:flex-start;gap:10px;padding:11px 0;border-top:1px solid var(--line);font-size:.8rem;line-height:1.5;}
.check-row input[type=checkbox]{width:18px;height:18px;flex-shrink:0;margin-top:1px;accent-color:var(--green);}
.check-row .yn{display:flex;gap:8px;margin-left:auto;}
.yn-btn{padding:6px 14px;border-radius:8px;border:1px solid var(--line);background:transparent;color:var(--mut);font-size:.72rem;font-family:'Space Mono';}
.yn-btn.on{border-color:var(--green);color:var(--green);background:rgba(34,197,94,.08);}
.yn-btn.on.no{border-color:var(--bad);color:#ff8a8a;background:rgba(255,93,93,.08);}
.legal-link{color:var(--blue);text-decoration:underline;cursor:pointer;}

.agreement{max-height:50vh;overflow:auto;background:#08090b;border:1px solid var(--line);border-radius:12px;padding:16px;font-size:.76rem;line-height:1.75;color:#c2c7cc;}
.agreement h4{font-family:'Space Mono';font-size:.74rem;color:#fff;margin:14px 0 6px;letter-spacing:.5px;}
.agreement p{margin-bottom:9px;}

.toast{position:fixed;bottom:24px;left:50%;transform:translateX(-50%) translateY(140%);background:#fff;color:#000;font-family:'Space Mono';font-size:.74rem;font-weight:700;padding:12px 20px;border-radius:12px;z-index:500;transition:.4s cubic-bezier(.2,.8,.2,1);box-shadow:0 8px 40px rgba(0,0,0,.5);max-width:90vw;text-align:center;}
.toast.show{transform:translateX(-50%) translateY(0);}
</style>
</head>
<body>

<!-- ============ LOGIN ============ -->
<div id="view-login">
  <div class="login-box">
    <div class="login-logo">
      <div class="disp">müzikHan</div>
      <div class="sub mono">STÜDYO YÖNETİM PANELİ</div>
    </div>

    <!-- normal login -->
    <div class="login-card" id="login-card">
      <h3>GİRİŞ YAP</h3>
      <div class="field"><span class="lbl">E-posta</span><input id="l-email" type="email" class="input" placeholder="mail@ornek.com" autocomplete="email"></div>
      <div class="field"><span class="lbl">Şifre</span><input id="l-pass" type="password" class="input" placeholder="••••••••" autocomplete="current-password"></div>
      <button id="l-submit" class="btn btn-primary">Giriş Yap</button>
      <div id="l-msg" class="msg"></div>
    </div>

    <!-- first-admin bootstrap (only shown when no users exist) -->
    <div class="login-card hidden" id="boot-card">
      <h3>İLK YÖNETİCİYİ OLUŞTUR</h3>
      <div class="eyebrow" style="margin-bottom:14px;line-height:1.6;">Sistemde hiç kullanıcı yok. İlk yönetici hesabını oluştur — diğer üyeleri sen ekleyeceksin.</div>
      <div class="field"><span class="lbl">İsim / Sahne Adı</span><input id="b-name" class="input" placeholder="örn. CESO"></div>
      <div class="field"><span class="lbl">E-posta</span><input id="b-email" type="email" class="input" placeholder="yonetici@mail.com"></div>
      <div class="field"><span class="lbl">Şifre (min 6)</span><input id="b-pass" type="password" class="input" placeholder="••••••••"></div>
      <button id="b-submit" class="btn btn-green">Yöneticiyi Oluştur</button>
      <div id="b-msg" class="msg"></div>
    </div>
  </div>
</div>

<!-- ============ APP ============ -->
<div id="view-app">
  <div class="topbar">
    <div class="topbar-in">
      <div class="brand">müzikHan<small>STÜDYO PANELİ</small></div>
      <div class="top-right">
        <button class="icon-btn" id="bell-btn">🔔<span class="badge-count hidden" id="notif-count">0</span></button>
        <div class="dropdown" id="notif-dd"><div class="notif-list" id="notif-list"></div></div>
        <button class="me-btn" id="me-btn">
          <div class="me-av" id="me-av">?</div>
          <div style="text-align:left;line-height:1.2;">
            <div class="me-name" id="me-name">—</div>
            <div class="role-chip" id="me-role"></div>
          </div>
        </button>
        <div class="dropdown" id="me-dd">
          <div class="dd-item" id="dd-settings">⚙️ Ayarlar</div>
          <div class="dd-sep"></div>
          <div class="dd-item danger" id="dd-logout">⏻ Oturumu kapat</div>
        </div>
      </div>
    </div>
  </div>

  <div class="nav"><div class="nav-in" id="nav-in"></div></div>

  <div class="wrap">
    <!-- DEMOLAR -->
    <div class="screen" id="s-demos">
      <div class="screen-head">
        <div><div class="eyebrow">A&amp;R · Değerlendirme</div><h2>Demolar</h2></div>
        <button class="btn btn-sm btn-primary" id="demo-add">+ Demo Yükle</button>
      </div>
      <div id="demos-list"><div class="center-load"><span class="spinner"></span></div></div>
    </div>

    <!-- RELEASE -->
    <div class="screen" id="s-release">
      <div class="screen-head">
        <div><div class="eyebrow">Dağıtım</div><h2>Release Ver</h2></div>
        <button class="btn btn-sm btn-green" id="release-add" style="width:auto;">+ Şarkı Yükle</button>
      </div>
      <div id="release-list"><div class="center-load"><span class="spinner"></span></div></div>
    </div>

    <!-- MUTFAK -->
    <div class="screen" id="s-mutfak">
      <div class="screen-head">
        <div><div class="eyebrow">Prodüksiyon</div><h2>Mutfak</h2></div>
        <div style="display:flex;gap:8px;">
          <button class="btn btn-sm btn-primary" id="beat-add" style="width:auto;">+ Beat</button>
          <button class="btn btn-sm btn-ghost" id="preset-add" style="width:auto;">+ Preset</button>
        </div>
      </div>
      <div class="nav-in" style="padding:0 0 14px;gap:8px;">
        <button class="nav-btn active" id="mt-beats" data-mt="beats">BEATLER</button>
        <button class="nav-btn" id="mt-presets" data-mt="presets">MIX PRESETLERİ</button>
      </div>
      <div id="beats-list"><div class="center-load"><span class="spinner"></span></div></div>
      <div id="presets-list" class="hidden"></div>
    </div>

    <!-- STEMS -->
    <div class="screen" id="s-stems">
      <div class="screen-head">
        <div><div class="eyebrow">Miks için</div><h2>Stems</h2></div>
        <button class="btn btn-sm btn-primary" id="stems-add" style="width:auto;">+ Stems Yükle</button>
      </div>
      <div id="stems-list"><div class="center-load"><span class="spinner"></span></div></div>
    </div>

    <!-- ADMIN -->
    <div class="screen" id="s-admin">
      <div class="screen-head">
        <div><div class="eyebrow">Yönetim</div><h2>Üyeler</h2></div>
        <button class="btn btn-sm btn-green" id="user-add" style="width:auto;">+ Üye Ekle</button>
      </div>
      <div id="users-list"></div>
    </div>
  </div>
</div>

<!-- ============ MODALS ============ -->
<!-- demo -->
<div class="modal-bg" id="m-demo"><div class="modal">
  <h3>Demo Yükle</h3>
  <div class="field"><span class="lbl">Şarkı Adı *</span><input id="dm-song" class="input" placeholder="örn. GECE VURUŞU" maxlength="90"></div>
  <div class="field"><span class="lbl">Demo İsmi</span><input id="dm-name" class="input" placeholder="çalışma ismi (opsiyonel)" maxlength="90"></div>
  <div class="row2">
    <div class="field"><span class="lbl">Beatmaker</span><input id="dm-beatmaker" class="input" placeholder="beatmaker"></div>
    <div class="field"><span class="lbl">Prodüktör (prod by)</span><input id="dm-producer" class="input" placeholder="prod ismi"></div>
  </div>
  <div class="field"><span class="lbl">Ses Dosyası (mp3/wav · max 40MB) *</span>
    <div class="filedrop" id="dm-drop"><div class="ic">⬆</div><div class="t" id="dm-drop-t">Dosya seç</div></div>
    <input id="dm-file" type="file" accept="audio/*,.mp3,.wav" class="hidden"></div>
  <div class="progress" id="dm-prog"><i></i></div><div class="msg" id="dm-msg"></div>
  <div class="modal-actions"><button class="btn btn-primary" id="dm-up">Yükle ve Paylaş</button><button class="btn btn-ghost modal-x">İptal</button></div>
</div></div>

<!-- beat -->
<div class="modal-bg" id="m-beat"><div class="modal">
  <h3>Beat Yükle</h3>
  <div class="field"><span class="lbl">Beat Adı *</span><input id="bt-title" class="input" placeholder="örn. PURPLE NIGHT" maxlength="90"></div>
  <div class="field"><span class="lbl">Prodüktör (prod by)</span><input id="bt-producer" class="input" placeholder="otomatik = senin adın"></div>
  <div class="field"><span class="lbl">Ses Dosyası (mp3/wav · max 80MB) *</span>
    <div class="filedrop" id="bt-drop"><div class="ic">⬆</div><div class="t" id="bt-drop-t">Dosya seç</div></div>
    <input id="bt-file" type="file" accept="audio/*,.mp3,.wav" class="hidden"></div>
  <div class="progress" id="bt-prog"><i></i></div><div class="msg" id="bt-msg"></div>
  <div class="modal-actions"><button class="btn btn-primary" id="bt-up">Yükle</button><button class="btn btn-ghost modal-x">İptal</button></div>
</div></div>

<!-- preset -->
<div class="modal-bg" id="m-preset"><div class="modal">
  <h3>Mix Preset Yükle</h3>
  <div class="field"><span class="lbl">Preset Adı *</span><input id="pr-title" class="input" placeholder="örn. Vokal Master Chain" maxlength="90"></div>
  <div class="field"><span class="lbl">Preset Tarifi</span><textarea id="pr-recipe" class="input" placeholder="Hangi plugin zinciri, ayarlar, ne için kullanılır..." maxlength="800"></textarea></div>
  <div class="field"><span class="lbl">Dosya (max 60MB) *</span>
    <div class="filedrop" id="pr-drop"><div class="ic">⬆</div><div class="t" id="pr-drop-t">Dosya seç (.fxp .fst .zip vb.)</div></div>
    <input id="pr-file" type="file" class="hidden"></div>
  <div class="progress" id="pr-prog"><i></i></div><div class="msg" id="pr-msg"></div>
  <div class="modal-actions"><button class="btn btn-primary" id="pr-up">Yükle</button><button class="btn btn-ghost modal-x">İptal</button></div>
</div></div>

<!-- stems -->
<div class="modal-bg" id="m-stems"><div class="modal">
  <h3>Stems Yükle</h3>
  <div class="field"><span class="lbl">Şarkı Adı *</span><input id="st-song" class="input" placeholder="şarkı adı" maxlength="90"></div>
  <div class="row2">
    <div class="field"><span class="lbl">BPM</span><input id="st-bpm" class="input" type="number" placeholder="140" min="40" max="300"></div>
    <div class="field"><span class="lbl">Ton (Key)</span><input id="st-key" class="input" placeholder="örn. F# minor"></div>
  </div>
  <div class="field"><span class="lbl">Stems Arşivi (.zip / .rar · max 500MB) *</span>
    <div class="filedrop" id="st-drop"><div class="ic">⬆</div><div class="t" id="st-drop-t">Arşiv seç</div></div>
    <input id="st-file" type="file" accept=".zip,.rar,application/zip,application/x-rar-compressed" class="hidden"></div>
  <div class="progress" id="st-prog"><i></i></div><div class="msg" id="st-msg"></div>
  <div class="modal-actions"><button class="btn btn-primary" id="st-up">Yükle</button><button class="btn btn-ghost modal-x">İptal</button></div>
</div></div>

<!-- release -->
<div class="modal-bg" id="m-release"><div class="modal">
  <h3>Şarkını Yayınla</h3>
  <div class="field"><span class="lbl">Sanatçı</span><input id="rl-artist" class="input" readonly style="opacity:.8;"></div>
  <div class="field"><span class="lbl">Şarkı İsmi *</span><input id="rl-song" class="input" placeholder="şarkı adı" maxlength="100"></div>
  <div class="field"><span class="lbl">Şarkı Kapağı — 3000×3000 (kare) *</span>
    <div class="filedrop" id="rl-cover-drop"><div class="ic">🖼</div><div class="t" id="rl-cover-t">Kapak seç</div></div>
    <input id="rl-cover" type="file" accept="image/*" class="hidden"></div>
  <div class="field"><span class="lbl">Şarkı Dosyası (wav/mp3 · max 25MB) *</span>
    <div class="filedrop" id="rl-audio-drop"><div class="ic">⬆</div><div class="t" id="rl-audio-t">Ses dosyası seç</div></div>
    <input id="rl-audio" type="file" accept="audio/*,.mp3,.wav" class="hidden"></div>
  <div class="field"><span class="lbl">Şarkı Sözleri</span><textarea id="rl-lyrics" class="input" placeholder="sözler..." maxlength="6000"></textarea></div>
  <div class="field"><span class="lbl">Yayınlanacağı Tarih *</span><input id="rl-date" type="date" class="input"></div>

  <div class="check-row">
    <div style="flex:1;">Şarkı sözlerinde explicit içerik var mı?</div>
    <div class="yn" data-yn="explicit"><button class="yn-btn" data-v="1">Evet</button><button class="yn-btn no" data-v="0">Hayır</button></div>
  </div>
  <div class="check-row">
    <div style="flex:1;">Şarkının beat lisansı bana ait mi?</div>
    <div class="yn" data-yn="license"><button class="yn-btn" data-v="1">Evet</button><button class="yn-btn no" data-v="0">Hayır</button></div>
  </div>
  <div class="check-row">
    <input type="checkbox" id="rl-agree">
    <div><span class="legal-link" id="open-agreement">Kullanıcı sözleşmesini</span> okudum, kabul ediyorum.</div>
  </div>

  <div class="msg" id="rl-msg"></div>
  <div class="progress" id="rl-prog"><i></i></div>
  <button class="btn btn-green" id="rl-up" style="margin-top:16px;">ŞARKINI YAYINLA</button>
  <button class="btn btn-ghost modal-x" style="margin-top:10px;">İptal</button>
</div></div>

<!-- agreement popup -->
<div class="modal-bg" id="m-agreement"><div class="modal">
  <h3>Dağıtım & Yayın Sözleşmesi</h3>
  <div class="agreement" id="agreement-body"></div>
  <div class="modal-actions"><button class="btn btn-primary modal-x">Okudum, kapat</button></div>
</div></div>

<!-- add user (admin) -->
<div class="modal-bg" id="m-user"><div class="modal">
  <h3>Yeni Üye Ekle</h3>
  <div class="field"><span class="lbl">İsim *</span><input id="u-name" class="input" placeholder="üye adı"></div>
  <div class="field"><span class="lbl">E-posta *</span><input id="u-email" type="email" class="input" placeholder="mail@ornek.com"></div>
  <div class="field"><span class="lbl">Şifre (min 6) *</span><input id="u-pass" type="text" class="input" placeholder="üyeye verilecek şifre"></div>
  <div class="field"><span class="lbl">Rol *</span>
    <select id="u-role" class="input">
      <option value="artist">Sanatçı</option>
      <option value="producer">Prodüktör</option>
      <option value="team">Ekip Üyesi</option>
      <option value="admin">Yönetici</option>
    </select></div>
  <div class="msg" id="u-msg"></div>
  <div class="modal-actions"><button class="btn btn-green" id="u-create">Üyeyi Oluştur</button><button class="btn btn-ghost modal-x">İptal</button></div>
</div></div>

<!-- settings -->
<div class="modal-bg" id="m-settings"><div class="modal">
  <h3>Ayarlar</h3>
  <div class="eyebrow" style="margin-bottom:10px;">Şifre Değiştir</div>
  <div class="field"><span class="lbl">Mevcut Şifre</span><input id="se-cur1" type="password" class="input" placeholder="••••••••"></div>
  <div class="field"><span class="lbl">Yeni Şifre (min 6)</span><input id="se-new" type="password" class="input" placeholder="••••••••"></div>
  <button class="btn btn-primary" id="se-pass-btn">Şifreyi Güncelle</button>
  <div class="msg" id="se-pass-msg"></div>
  <div style="height:1px;background:var(--line);margin:20px 0;"></div>
  <div class="eyebrow" style="margin-bottom:10px;">E-posta Değiştir</div>
  <div class="field"><span class="lbl">Mevcut Şifre</span><input id="se-cur2" type="password" class="input" placeholder="••••••••"></div>
  <div class="field"><span class="lbl">Yeni E-posta</span><input id="se-email" type="email" class="input" placeholder="yeni@mail.com"></div>
  <button class="btn btn-primary" id="se-email-btn">E-postayı Güncelle</button>
  <div class="msg" id="se-email-msg"></div>
  <div class="modal-actions"><button class="btn btn-ghost modal-x">Kapat</button></div>
</div></div>

<div class="toast" id="toast"></div>

<script type="module">
import { initializeApp } from "https://www.gstatic.com/firebasejs/12.15.0/firebase-app.js";
import { getAuth, signInWithEmailAndPassword, signOut, onAuthStateChanged,
  createUserWithEmailAndPassword, updateProfile, EmailAuthProvider,
  reauthenticateWithCredential, updatePassword, verifyBeforeUpdateEmail }
  from "https://www.gstatic.com/firebasejs/12.15.0/firebase-auth.js";
import { getFirestore, doc, getDoc, setDoc, updateDoc, deleteDoc, collection,
  addDoc, getDocs, query, where, orderBy, onSnapshot, serverTimestamp, increment, limit }
  from "https://www.gstatic.com/firebasejs/12.15.0/firebase-firestore.js";
import { getStorage, ref, uploadBytesResumable, getDownloadURL, deleteObject }
  from "https://www.gstatic.com/firebasejs/12.15.0/firebase-storage.js";

const firebaseConfig = {
  apiKey: "AIzaSyBAEfpdzpoB2vtvhSDOZaGuSn24TCZjmsI",
  authDomain: "ceso-istanbul.firebaseapp.com",
  projectId: "ceso-istanbul",
  storageBucket: "ceso-istanbul.firebasestorage.app",
  messagingSenderId: "30392774343",
  appId: "1:30392774343:web:8baffdf90c6e97404858e2",
  measurementId: "G-24YEGLXWTD"
};
const app = initializeApp(firebaseConfig);
const auth = getAuth(app);
const db = getFirestore(app);
const storage = getStorage(app);
// ikinci uygulama: admin yeni üye oluştururken kendi oturumu düşmesin diye
const creatorApp = initializeApp(firebaseConfig, "creator");
const creatorAuth = getAuth(creatorApp);

/* ---------- helpers ---------- */
const $ = s => document.querySelector(s);
const esc = s => (s||"").toString().replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const initial = n => (n||"?").trim().charAt(0).toUpperCase();
function toast(t){const e=$("#toast");e.textContent=t;e.classList.add("show");setTimeout(()=>e.classList.remove("show"),2800);}
function timeAgo(ts){ if(!ts) return ""; const d=ts.toDate?ts.toDate():new Date(ts); const s=Math.floor((Date.now()-d.getTime())/1000);
  if(s<60)return "az önce"; if(s<3600)return Math.floor(s/60)+" dk önce"; if(s<86400)return Math.floor(s/3600)+" sa önce"; return Math.floor(s/86400)+" gün önce"; }
function fdate(ts){ if(!ts) return ""; const d=ts.toDate?ts.toDate():new Date(ts); return d.toLocaleDateString("tr-TR"); }
function mb(b){return (b/1024/1024).toFixed(1)+"MB";}

const ROLE_LABEL={admin:"Yönetici",team:"Ekip Üyesi",artist:"Sanatçı",producer:"Prodüktör"};
const ROLE_CLASS={admin:"role-admin",team:"role-team",artist:"role-artist",producer:"role-producer"};

let me=null, myUid=null, myRole=null, myName=null;

/* ---------- LOGIN ---------- */
(async function checkBootstrap(){
  // kurulum bayrağı yoksa "ilk yönetici" ekranını göster (giriş gerektirmeden)
  try{
    const s=await getDoc(doc(db,"meta","setup"));
    if(!s.exists()){ $("#login-card").classList.add("hidden"); $("#boot-card").classList.remove("hidden"); }
  }catch(e){ /* okuma kapalıysa normal login kalır */ }
})();

$("#l-submit").onclick=async ()=>{
  const m=$("#l-msg"); m.className="msg";
  const email=$("#l-email").value.trim(), pass=$("#l-pass").value;
  if(!email||!pass){ m.classList.add("err"); m.textContent="E-posta ve şifre gir."; return; }
  $("#l-submit").disabled=true;
  try{ await signInWithEmailAndPassword(auth,email,pass); }
  catch(e){ m.classList.add("err"); m.textContent=authErr(e.code); }
  $("#l-submit").disabled=false;
};
$("#l-pass").addEventListener("keydown",e=>{if(e.key==="Enter")$("#l-submit").click();});

$("#b-submit").onclick=async ()=>{
  const m=$("#b-msg"); m.className="msg";
  const name=$("#b-name").value.trim(), email=$("#b-email").value.trim(), pass=$("#b-pass").value;
  if(!name||!email||!pass){ m.classList.add("err"); m.textContent="Tüm alanları doldur."; return; }
  $("#b-submit").disabled=true;
  try{
    const cred=await createUserWithEmailAndPassword(auth,email,pass);
    await updateProfile(cred.user,{displayName:name});
    await setDoc(doc(db,"users",cred.user.uid),{displayName:name,email,role:"admin",createdAt:serverTimestamp()});
    await setDoc(doc(db,"meta","setup"),{done:true,createdAt:serverTimestamp()}).catch(()=>{});
    // onAuthStateChanged devralacak
  }catch(e){ m.classList.add("err"); m.textContent=authErr(e.code); $("#b-submit").disabled=false; }
};

function authErr(c){
  const m={"auth/invalid-email":"Geçersiz e-posta.","auth/email-already-in-use":"Bu e-posta zaten kayıtlı.","auth/weak-password":"Şifre en az 6 karakter olmalı.","auth/invalid-credential":"E-posta veya şifre hatalı.","auth/user-not-found":"Kullanıcı bulunamadı.","auth/wrong-password":"Şifre hatalı.","auth/too-many-requests":"Çok fazla deneme, biraz bekle.","auth/requires-recent-login":"Bu işlem için tekrar giriş yapman gerekiyor."};
  return m[c]||("Hata: "+c);
}

/* ---------- AUTH STATE ---------- */
onAuthStateChanged(auth, async user=>{
  if(!user){ me=null; $("#view-app").style.display="none"; $("#view-login").style.display="flex"; return; }
  me=user; myUid=user.uid;
  let snap=await getDoc(doc(db,"users",user.uid));
  if(!snap.exists()){
    // güvenlik: profili olmayan kullanıcı (admin henüz rol atamamış) -> çıkış
    await signOut(auth); $("#l-msg").className="msg err"; $("#l-msg").textContent="Hesabın henüz yetkilendirilmemiş. Yönetici ile görüş."; return;
  }
  const d=snap.data(); myRole=d.role; myName=d.displayName||user.email;
  enterApp();
});

const NAV=[
  {id:"demos",label:"DEMOLAR",roles:["admin","team"]},
  {id:"release",label:"RELEASE",roles:["admin","team","artist"]},
  {id:"mutfak",label:"MUTFAK",roles:["admin","team","producer"]},
  {id:"stems",label:"STEMS",roles:["admin","team","producer"]},
  {id:"admin",label:"YÖNETİM",roles:["admin"]},
];
function canSee(id){ const n=NAV.find(x=>x.id===id); return n && n.roles.includes(myRole); }

function enterApp(){
  $("#view-login").style.display="none"; $("#view-app").style.display="block";
  $("#me-av").textContent=initial(myName); $("#me-name").textContent=myName;
  $("#me-role").textContent=ROLE_LABEL[myRole]||myRole;
  // nav
  const nav=$("#nav-in"); nav.innerHTML="";
  const allowed=NAV.filter(n=>n.roles.includes(myRole));
  allowed.forEach(n=>{ const b=document.createElement("button"); b.className="nav-btn"; b.dataset.screen=n.id; b.textContent=n.label;
    b.onclick=()=>showScreen(n.id); nav.appendChild(b); });
  listenNotifications();
  if(allowed[0]) showScreen(allowed[0].id);
}

function showScreen(id){
  document.querySelectorAll(".nav-btn[data-screen]").forEach(b=>b.classList.toggle("active",b.dataset.screen===id));
  document.querySelectorAll(".screen").forEach(s=>s.classList.remove("active"));
  $("#s-"+id).classList.add("active");
  if(id==="demos") loadDemos();
  if(id==="release") loadReleases();
  if(id==="mutfak"){ loadBeats(); }
  if(id==="stems") loadStems();
  if(id==="admin") loadUsers();
  closeDropdowns();
}

/* ---------- dropdowns ---------- */
function closeDropdowns(){ $("#notif-dd").classList.remove("open"); $("#me-dd").classList.remove("open"); }
$("#me-btn").onclick=e=>{ e.stopPropagation(); $("#notif-dd").classList.remove("open"); $("#me-dd").classList.toggle("open"); };
$("#bell-btn").onclick=e=>{ e.stopPropagation(); $("#me-dd").classList.remove("open"); $("#notif-dd").classList.toggle("open"); if($("#notif-dd").classList.contains("open")) markNotifsRead(); };
document.addEventListener("click",closeDropdowns);
$("#dd-logout").onclick=()=>signOut(auth);
$("#dd-settings").onclick=()=>openModal("m-settings");

/* ---------- NOTIFICATIONS ---------- */
let notifUnsub=null, notifDocs=[];
function listenNotifications(){
  if(notifUnsub) notifUnsub();
  const qy=query(collection(db,"notifications"),where("toUid","==",myUid),orderBy("createdAt","desc"),limit(40));
  notifUnsub=onSnapshot(qy,snap=>{
    notifDocs=[]; snap.forEach(d=>notifDocs.push({id:d.id,...d.data()}));
    const unread=notifDocs.filter(n=>!n.read).length;
    const c=$("#notif-count"); if(unread>0){c.textContent=unread;c.classList.remove("hidden");}else c.classList.add("hidden");
    renderNotifs();
  },()=>{});
}
function renderNotifs(){
  const el=$("#notif-list");
  if(!notifDocs.length){ el.innerHTML=`<div class="notif-empty">Henüz bildirim yok.</div>`; return; }
  el.innerHTML=notifDocs.map(n=>`<div class="notif ${n.read?'':'unread'}">${esc(n.text)}<div class="t">${timeAgo(n.createdAt)}</div></div>`).join("");
}
async function markNotifsRead(){
  const un=notifDocs.filter(n=>!n.read);
  await Promise.all(un.map(n=>updateDoc(doc(db,"notifications",n.id),{read:true}).catch(()=>{})));
}
async function notify(uid,text){ if(!uid||uid===myUid) return; await addDoc(collection(db,"notifications"),{toUid:uid,text,read:false,createdAt:serverTimestamp()}); }
async function notifyRole(role,text){ const us=await getDocs(query(collection(db,"users"),where("role","==",role))); await Promise.all(us.docs.map(u=>notify(u.id,text))); }

/* ---------- generic modal ---------- */
function openModal(id){ $("#"+id).classList.add("open"); }
function closeModal(id){ $("#"+id).classList.remove("open"); }
document.querySelectorAll(".modal-x").forEach(b=>b.onclick=e=>e.target.closest(".modal-bg").classList.remove("open"));
document.querySelectorAll(".modal-bg").forEach(bg=>bg.addEventListener("click",e=>{ if(e.target===bg) bg.classList.remove("open"); }));

/* ---------- file picker wiring ---------- */
function wireDrop(dropId,inputId,textId,onPick){
  $("#"+dropId).onclick=()=>$("#"+inputId).click();
  $("#"+inputId).onchange=e=>{ const f=e.target.files[0]; if(!f)return; onPick(f); };
}
function setDrop(dropId,textId,f,extra){ $("#"+dropId).classList.add("has"); $("#"+textId).textContent=f.name+" · "+mb(f.size)+(extra||""); }

async function uploadFile(path,file,onProg){
  const sref=ref(storage,path);
  const task=uploadBytesResumable(sref,file,{contentType:file.type||"application/octet-stream"});
  task.on("state_changed",s=>onProg&&onProg(Math.round(s.bytesTransferred/s.totalBytes*100)));
  await task;
  return {url:await getDownloadURL(sref),path};
}

/* ===================================================== DEMOS ===================================================== */
let demoFile=null;
wireDrop("dm-drop","dm-file","dm-drop-t",f=>{ if(f.size>40*1024*1024){$("#dm-msg").className="msg err";$("#dm-msg").textContent="Max 40MB.";return;} demoFile=f; setDrop("dm-drop","dm-drop-t",f); $("#dm-msg").textContent=""; });
$("#demo-add").onclick=()=>{ demoFile=null; ["dm-song","dm-name","dm-beatmaker","dm-producer"].forEach(i=>$("#"+i).value=""); $("#dm-drop").classList.remove("has"); $("#dm-drop-t").textContent="Dosya seç"; $("#dm-msg").textContent=""; $("#dm-prog").classList.remove("show"); openModal("m-demo"); };
$("#dm-up").onclick=async ()=>{
  const song=$("#dm-song").value.trim(), msg=$("#dm-msg"); msg.className="msg";
  if(!song){msg.classList.add("err");msg.textContent="Şarkı adı gir.";return;}
  if(!demoFile){msg.classList.add("err");msg.textContent="Ses dosyası seç.";return;}
  $("#dm-up").disabled=true; $("#dm-prog").classList.add("show"); const bar=$("#dm-prog").querySelector("i");
  try{
    const safe=demoFile.name.replace(/[^\w.\-]/g,"_");
    const {url,path}=await uploadFile(`demos/${myUid}/${Date.now()}_${safe}`,demoFile,p=>bar.style.width=p+"%");
    await addDoc(collection(db,"demos"),{songName:song,demoName:$("#dm-name").value.trim(),beatmaker:$("#dm-beatmaker").value.trim(),producer:$("#dm-producer").value.trim(),
      audioURL:url,storagePath:path,uploaderId:myUid,uploaderName:myName,createdAt:serverTimestamp(),sumSong:0,sumLyrics:0,sumMix:0,ratingCount:0});
    closeModal("m-demo"); toast("Demo paylaşıldı 🔥"); loadDemos();
  }catch(e){msg.classList.add("err");msg.textContent="Hata: "+(e.code||e.message);}
  $("#dm-up").disabled=false;
};

async function loadDemos(){
  const box=$("#demos-list"); box.innerHTML=`<div class="center-load"><span class="spinner"></span></div>`;
  try{
    const snap=await getDocs(query(collection(db,"demos"),orderBy("createdAt","desc")));
    if(snap.empty){box.innerHTML=emptyState("🎧","Henüz demo yok.");return;}
    box.innerHTML=""; snap.forEach(d=>box.appendChild(demoCard(d.id,d.data())));
  }catch(e){box.innerHTML=emptyState("⚠","Yüklenemedi: "+e.code);}
}
function emptyState(ic,t){return `<div class="empty-state"><div class="ic">${ic}</div><div class="t">${esc(t)}</div></div>`;}

const DEMO_CATS=[["Şarkı","song","sumSong"],["Sözler","lyrics","sumLyrics"],["Mix & Master","mix","sumMix"]];
function avg(d,sk){return d.ratingCount?(d[sk]/d.ratingCount):0;}
function sumStars(v){let h='<span class="summary-stars">';for(let i=1;i<=5;i++)h+=`<span class="s ${i<=Math.round(v)?'on':''}">★</span>`;return h+'</span>';}

function demoCard(id,d){
  const el=document.createElement("div");el.className="card-item";
  const overall=d.ratingCount?((avg(d,"sumSong")+avg(d,"sumLyrics")+avg(d,"sumMix"))/3).toFixed(1):null;
  const canDel=myRole==="admin"||d.uploaderId===myUid;
  el.innerHTML=`
    <div class="ci-head">
      <div class="ci-title">${esc(d.songName)}</div>
      <div class="ci-meta">
        <span>${esc(d.uploaderName)}</span><span>·</span><span>${fdate(d.createdAt)}</span>
        ${d.beatmaker?`<span>· beat: ${esc(d.beatmaker)}</span>`:""}
      </div>
      <div class="ci-tags">
        ${overall?`<span class="tag score">★ ${overall}/5 · ${d.ratingCount} oy</span>`:`<span class="tag">puanlanmadı</span>`}
        ${d.demoName?`<span class="tag">${esc(d.demoName)}</span>`:""}
      </div>
    </div>
    <audio controls preload="none" src="${esc(d.audioURL)}"></audio>
    ${d.producer?`<div class="ci-prod">prod by. ${esc(d.producer)}</div>`:""}
    <div class="ci-actions">
      <button class="linklike act-rate">★ PUANLA / NOT</button>
      <a class="dl" href="${esc(d.audioURL)}" download>⬇ indir</a>
      ${canDel?`<button class="linklike del act-del" style="margin-left:auto;">SİL</button>`:""}
    </div>
    <div class="drawer"></div>`;
  el.querySelector(".act-rate").onclick=()=>rateDrawer(el,"demos",id,d,DEMO_CATS,"Demo");
  if(canDel) el.querySelector(".act-del").onclick=()=>delItem("demos",id,d,loadDemos);
  return el;
}

/* generic rate drawer used by demos & beats */
function starsInput(cat,val){let h=`<div class="stars" data-cat="${cat}">`;for(let i=1;i<=5;i++)h+=`<span class="star ${i<=val?'on':''}" data-v="${i}">★</span>`;return h+`</div>`;}
async function rateDrawer(el,col,id,d,cats,kind){
  const dr=el.querySelector(".drawer");
  if(dr.classList.contains("open")){dr.classList.remove("open");return;}
  dr.classList.add("open"); dr.innerHTML=`<div class="center-load"><span class="spinner"></span></div>`;
  let prev={}; const ps=await getDoc(doc(db,col,id,"ratings",myUid)); if(ps.exists())prev=ps.data();
  dr.innerHTML=`<div class="eyebrow" style="margin-bottom:12px;">${kind} değerlendirme · 1–5</div>
    <div class="rate-grid">${cats.map(([label,k])=>`<div class="rate-cell"><div class="k">${label}</div>${starsInput(k,prev[k]||0)}</div>`).join("")}</div>
    <div class="field"><span class="lbl">Not</span><textarea class="input rate-note" placeholder="görüşün... (yükleyene iletilecek)" maxlength="500">${esc(prev.note||"")}</textarea></div>
    <button class="btn btn-primary r-save">${ps.exists()?"Puanı Güncelle":"Puanı Gönder"}</button>`;
  dr.querySelectorAll(".stars").forEach(sw=>{ sw.querySelectorAll(".star").forEach(st=>{ st.onclick=()=>{ const v=+st.dataset.v; sw.dataset.val=v; sw.querySelectorAll(".star").forEach(x=>x.classList.toggle("on",+x.dataset.v<=v)); }; }); if(prev[sw.dataset.cat])sw.dataset.val=prev[sw.dataset.cat]; });
  dr.querySelector(".r-save").onclick=async ()=>{
    const vals={}; let ok=true;
    cats.forEach(([_l,k])=>{ const sw=dr.querySelector(`.stars[data-cat="${k}"]`); vals[k]=+(sw.dataset.val||0); if(!vals[k])ok=false; });
    if(!ok){toast("Tüm kategorileri puanla");return;}
    const note=dr.querySelector(".rate-note").value.trim();
    await submitRating(col,id,d,cats,vals,note,ps.exists()?ps.data():null);
    dr.classList.remove("open"); toast("Puanın kaydedildi");
  };
}
async function submitRating(col,id,d,cats,vals,note,old){
  const upd={}; cats.forEach(([_l,k,sk])=>{ upd[sk]=increment(vals[k]-(old?old[k]||0:0)); });
  if(!old) upd.ratingCount=increment(1);
  await updateDoc(doc(db,col,id),upd);
  await setDoc(doc(db,col,id,"ratings",myUid),{...vals,note,raterName:myName,updatedAt:serverTimestamp()});
  // bildirim
  const scoreTxt=cats.map(([l,k])=>`${l}:${vals[k]}`).join(" · ");
  await notify(d.uploaderId,`${myName} "${d.songName||d.title}" için puan verdi (${scoreTxt})${note?` — "${note}"`:""}`);
  if(col==="demos")loadDemos(); if(col==="beats")loadBeats();
}

async function delItem(col,id,d,after){
  if(!confirm(`"${d.songName||d.title}" silinsin mi?`))return;
  try{ if(d.storagePath){try{await deleteObject(ref(storage,d.storagePath));}catch(_){}}
    await deleteDoc(doc(db,col,id)); toast("Silindi"); after&&after(); }
  catch(e){toast("Silinemedi: "+(e.code||e.message));}
}

/* ===================================================== MUTFAK: BEATS ===================================================== */
$("#mt-beats").onclick=()=>{ $("#mt-beats").classList.add("active"); $("#mt-presets").classList.remove("active"); $("#beats-list").classList.remove("hidden"); $("#presets-list").classList.add("hidden"); loadBeats(); };
$("#mt-presets").onclick=()=>{ $("#mt-presets").classList.add("active"); $("#mt-beats").classList.remove("active"); $("#presets-list").classList.remove("hidden"); $("#beats-list").classList.add("hidden"); loadPresets(); };

let beatFile=null;
wireDrop("bt-drop","bt-file","bt-drop-t",f=>{ if(f.size>80*1024*1024){$("#bt-msg").className="msg err";$("#bt-msg").textContent="Max 80MB.";return;} beatFile=f; setDrop("bt-drop","bt-drop-t",f); $("#bt-msg").textContent=""; });
$("#beat-add").onclick=()=>{ beatFile=null; $("#bt-title").value=""; $("#bt-producer").value=myName; $("#bt-drop").classList.remove("has"); $("#bt-drop-t").textContent="Dosya seç"; $("#bt-msg").textContent=""; $("#bt-prog").classList.remove("show"); openModal("m-beat"); };
$("#bt-up").onclick=async ()=>{
  const t=$("#bt-title").value.trim(),msg=$("#bt-msg");msg.className="msg";
  if(!t){msg.classList.add("err");msg.textContent="Beat adı gir.";return;}
  if(!beatFile){msg.classList.add("err");msg.textContent="Dosya seç.";return;}
  $("#bt-up").disabled=true;$("#bt-prog").classList.add("show");const bar=$("#bt-prog").querySelector("i");
  try{
    const safe=beatFile.name.replace(/[^\w.\-]/g,"_");
    const {url,path}=await uploadFile(`beats/${myUid}/${Date.now()}_${safe}`,beatFile,p=>bar.style.width=p+"%");
    await addDoc(collection(db,"beats"),{title:t,producer:$("#bt-producer").value.trim()||myName,audioURL:url,storagePath:path,uploaderId:myUid,uploaderName:myName,createdAt:serverTimestamp(),sumType:0,sumVibe:0,sumMelody:0,sumDrum:0,ratingCount:0});
    closeModal("m-beat");toast("Beat yüklendi 🔥");loadBeats();
  }catch(e){msg.classList.add("err");msg.textContent="Hata: "+(e.code||e.message);}
  $("#bt-up").disabled=false;
};
const BEAT_CATS=[["Type","type","sumType"],["Vibe","vibe","sumVibe"],["Melody","melody","sumMelody"],["Drum","drum","sumDrum"]];
async function loadBeats(){
  const box=$("#beats-list");box.innerHTML=`<div class="center-load"><span class="spinner"></span></div>`;
  try{ const snap=await getDocs(query(collection(db,"beats"),orderBy("createdAt","desc")));
    if(snap.empty){box.innerHTML=emptyState("🥁","Henüz beat yok.");return;}
    box.innerHTML="";snap.forEach(d=>box.appendChild(beatCard(d.id,d.data())));
  }catch(e){box.innerHTML=emptyState("⚠","Yüklenemedi: "+e.code);}
}
function beatCard(id,d){
  const el=document.createElement("div");el.className="card-item";
  const overall=d.ratingCount?((avg(d,"sumType")+avg(d,"sumVibe")+avg(d,"sumMelody")+avg(d,"sumDrum"))/4).toFixed(1):null;
  const canDel=myRole==="admin"||d.uploaderId===myUid;
  el.innerHTML=`
    <div class="ci-head"><div class="ci-title">${esc(d.title)}</div>
      <div class="ci-meta"><span>${esc(d.uploaderName)}</span><span>·</span><span>${fdate(d.createdAt)}</span></div>
      <div class="ci-tags">${overall?`<span class="tag score">★ ${overall}/5 · ${d.ratingCount} oy</span>`:`<span class="tag">puanlanmadı</span>`}</div>
    </div>
    <audio controls preload="none" src="${esc(d.audioURL)}"></audio>
    ${d.producer?`<div class="ci-prod">prod by. ${esc(d.producer)}</div>`:""}
    <div class="ci-actions"><button class="linklike act-rate">★ PUANLA / NOT</button>
      <a class="dl" href="${esc(d.audioURL)}" download>⬇ indir</a>
      ${canDel?`<button class="linklike del act-del" style="margin-left:auto;">SİL</button>`:""}</div>
    <div class="drawer"></div>`;
  el.querySelector(".act-rate").onclick=()=>rateDrawer(el,"beats",id,d,BEAT_CATS,"Beat");
  if(canDel)el.querySelector(".act-del").onclick=()=>delItem("beats",id,d,loadBeats);
  return el;
}

/* ---------- PRESETS ---------- */
let presetFile=null;
wireDrop("pr-drop","pr-file","pr-drop-t",f=>{ if(f.size>60*1024*1024){$("#pr-msg").className="msg err";$("#pr-msg").textContent="Max 60MB.";return;} presetFile=f; setDrop("pr-drop","pr-drop-t",f); $("#pr-msg").textContent=""; });
$("#preset-add").onclick=()=>{ presetFile=null; $("#pr-title").value="";$("#pr-recipe").value=""; $("#pr-drop").classList.remove("has");$("#pr-drop-t").textContent="Dosya seç (.fxp .fst .zip vb.)";$("#pr-msg").textContent="";$("#pr-prog").classList.remove("show"); openModal("m-preset"); };
$("#pr-up").onclick=async ()=>{
  const t=$("#pr-title").value.trim(),msg=$("#pr-msg");msg.className="msg";
  if(!t){msg.classList.add("err");msg.textContent="Preset adı gir.";return;}
  if(!presetFile){msg.classList.add("err");msg.textContent="Dosya seç.";return;}
  $("#pr-up").disabled=true;$("#pr-prog").classList.add("show");const bar=$("#pr-prog").querySelector("i");
  try{
    const safe=presetFile.name.replace(/[^\w.\-]/g,"_");
    const {url,path}=await uploadFile(`presets/${myUid}/${Date.now()}_${safe}`,presetFile,p=>bar.style.width=p+"%");
    await addDoc(collection(db,"presets"),{title:t,recipe:$("#pr-recipe").value.trim(),fileURL:url,fileName:presetFile.name,storagePath:path,uploaderId:myUid,uploaderName:myName,createdAt:serverTimestamp()});
    closeModal("m-preset");toast("Preset yüklendi");loadPresets();$("#mt-presets").click();
  }catch(e){msg.classList.add("err");msg.textContent="Hata: "+(e.code||e.message);}
  $("#pr-up").disabled=false;
};
async function loadPresets(){
  const box=$("#presets-list");box.innerHTML=`<div class="center-load"><span class="spinner"></span></div>`;
  try{ const snap=await getDocs(query(collection(db,"presets"),orderBy("createdAt","desc")));
    if(snap.empty){box.innerHTML=emptyState("🎚","Henüz preset yok.");return;}
    box.innerHTML="";
    snap.forEach(s=>{const d=s.data();const canDel=myRole==="admin"||d.uploaderId===myUid;
      const el=document.createElement("div");el.className="card-item";
      el.innerHTML=`<div class="ci-head"><div class="ci-title">${esc(d.title)}</div>
        <div class="ci-meta"><span>${esc(d.uploaderName)}</span><span>·</span><span>${fdate(d.createdAt)}</span></div>
        ${d.recipe?`<div style="font-size:.8rem;color:#bdbdbd;line-height:1.6;margin-top:10px;white-space:pre-wrap;">${esc(d.recipe)}</div>`:""}</div>
        <div class="ci-actions"><a class="dl" href="${esc(d.fileURL)}" download>⬇ ${esc(d.fileName||"dosya")}</a>
        ${canDel?`<button class="linklike del act-del" style="margin-left:auto;">SİL</button>`:""}</div>`;
      if(canDel)el.querySelector(".act-del").onclick=()=>delItem("presets",s.id,d,loadPresets);
      box.appendChild(el);
    });
  }catch(e){box.innerHTML=emptyState("⚠","Yüklenemedi: "+e.code);}
}

/* ===================================================== STEMS ===================================================== */
let stemFile=null;
wireDrop("st-drop","st-file","st-drop-t",f=>{ const ok=/\.(zip|rar)$/i.test(f.name); if(!ok){$("#st-msg").className="msg err";$("#st-msg").textContent="Sadece .zip veya .rar";return;} if(f.size>500*1024*1024){$("#st-msg").className="msg err";$("#st-msg").textContent="Max 500MB.";return;} stemFile=f; setDrop("st-drop","st-drop-t",f); $("#st-msg").textContent=""; });
$("#stems-add").onclick=()=>{ stemFile=null; $("#st-song").value="";$("#st-bpm").value="";$("#st-key").value=""; $("#st-drop").classList.remove("has");$("#st-drop-t").textContent="Arşiv seç";$("#st-msg").textContent="";$("#st-prog").classList.remove("show"); openModal("m-stems"); };
$("#st-up").onclick=async ()=>{
  const song=$("#st-song").value.trim(),msg=$("#st-msg");msg.className="msg";
  if(!song){msg.classList.add("err");msg.textContent="Şarkı adı gir.";return;}
  if(!stemFile){msg.classList.add("err");msg.textContent="Arşiv seç.";return;}
  $("#st-up").disabled=true;$("#st-prog").classList.add("show");const bar=$("#st-prog").querySelector("i");
  try{
    const safe=stemFile.name.replace(/[^\w.\-]/g,"_");
    const {url,path}=await uploadFile(`stems/${myUid}/${Date.now()}_${safe}`,stemFile,p=>bar.style.width=p+"%");
    await addDoc(collection(db,"stems"),{songName:song,bpm:Number($("#st-bpm").value)||null,songKey:$("#st-key").value.trim(),fileURL:url,fileName:stemFile.name,storagePath:path,uploaderId:myUid,uploaderName:myName,createdAt:serverTimestamp()});
    await notifyRole("producer",`${myName} yeni stems yükledi: "${song}"${$("#st-bpm").value?` · ${$("#st-bpm").value} BPM`:""}${$("#st-key").value?` · ${$("#st-key").value}`:""}`);
    closeModal("m-stems");toast("Stems yüklendi · prodüktörlere bildirim gitti");loadStems();
  }catch(e){msg.classList.add("err");msg.textContent="Hata: "+(e.code||e.message);}
  $("#st-up").disabled=false;
};
async function loadStems(){
  const box=$("#stems-list");box.innerHTML=`<div class="center-load"><span class="spinner"></span></div>`;
  try{ const snap=await getDocs(query(collection(db,"stems"),orderBy("createdAt","desc")));
    if(snap.empty){box.innerHTML=emptyState("🗂","Henüz stems yok.");return;}
    box.innerHTML="";
    snap.forEach(s=>{const d=s.data();const canDel=myRole==="admin"||d.uploaderId===myUid;
      const el=document.createElement("div");el.className="card-item";
      el.innerHTML=`<div class="ci-head"><div class="ci-title">${esc(d.songName)}</div>
        <div class="ci-meta"><span>${esc(d.uploaderName)}</span><span>·</span><span>${fdate(d.createdAt)}</span></div>
        <div class="ci-tags">${d.bpm?`<span class="tag">${d.bpm} BPM</span>`:""}${d.songKey?`<span class="tag">${esc(d.songKey)}</span>`:""}</div></div>
        <div class="ci-actions"><a class="dl" href="${esc(d.fileURL)}" download>⬇ ${esc(d.fileName||"stems")}</a>
        ${canDel?`<button class="linklike del act-del" style="margin-left:auto;">SİL</button>`:""}</div>`;
      if(canDel)el.querySelector(".act-del").onclick=()=>delItem("stems",s.id,{...d,songName:d.songName},loadStems);
      box.appendChild(el);
    });
  }catch(e){box.innerHTML=emptyState("⚠","Yüklenemedi: "+e.code);}
}

/* ===================================================== RELEASE ===================================================== */
let rlCover=null, rlAudio=null, rlYN={explicit:null,license:null};
function checkSquare(file){return new Promise(res=>{const img=new Image();img.onload=()=>{res({w:img.naturalWidth,h:img.naturalHeight});URL.revokeObjectURL(img.src);};img.onerror=()=>res(null);img.src=URL.createObjectURL(file);});}
wireDrop("rl-cover-drop","rl-cover","rl-cover-t",async f=>{
  if(!f.type.startsWith("image/")){$("#rl-msg").className="msg err";$("#rl-msg").textContent="Kapak bir görsel olmalı.";return;}
  if(f.size>15*1024*1024){$("#rl-msg").className="msg err";$("#rl-msg").textContent="Kapak max 15MB.";return;}
  const dim=await checkSquare(f);
  if(!dim||dim.w!==dim.h||dim.w<3000){$("#rl-msg").className="msg err";$("#rl-msg").textContent=`Kapak kare ve en az 3000×3000 olmalı (seçilen: ${dim?dim.w+"×"+dim.h:"?"}).`;rlCover=null;$("#rl-cover-drop").classList.remove("has");$("#rl-cover-t").textContent="Kapak seç";return;}
  rlCover=f; setDrop("rl-cover-drop","rl-cover-t",f,` · ${dim.w}×${dim.h}`); $("#rl-msg").textContent="";
});
wireDrop("rl-audio-drop","rl-audio","rl-audio-t",f=>{ if(f.size>25*1024*1024){$("#rl-msg").className="msg err";$("#rl-msg").textContent="Şarkı dosyası max 25MB.";return;} rlAudio=f; setDrop("rl-audio-drop","rl-audio-t",f); $("#rl-msg").textContent=""; });
document.querySelectorAll('.yn').forEach(g=>g.querySelectorAll('.yn-btn').forEach(b=>b.onclick=()=>{ g.querySelectorAll('.yn-btn').forEach(x=>x.classList.remove('on')); b.classList.add('on'); rlYN[g.dataset.yn]=b.dataset.v==="1"; }));
$("#open-agreement").onclick=()=>openModal("m-agreement");
$("#release-add").onclick=()=>{
  rlCover=null;rlAudio=null;rlYN={explicit:null,license:null};
  $("#rl-artist").value=myName; ["rl-song","rl-lyrics","rl-date"].forEach(i=>$("#"+i).value=""); $("#rl-agree").checked=false;
  document.querySelectorAll('.yn-btn').forEach(b=>b.classList.remove('on'));
  $("#rl-cover-drop").classList.remove("has");$("#rl-cover-t").textContent="Kapak seç";
  $("#rl-audio-drop").classList.remove("has");$("#rl-audio-t").textContent="Ses dosyası seç";
  $("#rl-msg").textContent="";$("#rl-prog").classList.remove("show");
  openModal("m-release");
};
$("#rl-up").onclick=async ()=>{
  const msg=$("#rl-msg");msg.className="msg";
  const song=$("#rl-song").value.trim(), date=$("#rl-date").value;
  if(!song){msg.classList.add("err");msg.textContent="Şarkı ismi gir.";return;}
  if(!rlCover){msg.classList.add("err");msg.textContent="3000×3000 kare kapak yükle.";return;}
  if(!rlAudio){msg.classList.add("err");msg.textContent="Şarkı dosyası yükle.";return;}
  if(!date){msg.classList.add("err");msg.textContent="Yayın tarihi seç.";return;}
  if(rlYN.explicit===null){msg.classList.add("err");msg.textContent="Explicit sorusunu yanıtla.";return;}
  if(rlYN.license===null){msg.classList.add("err");msg.textContent="Beat lisansı sorusunu yanıtla.";return;}
  if(!$("#rl-agree").checked){msg.classList.add("err");msg.textContent="Sözleşmeyi kabul etmelisin.";return;}
  $("#rl-up").disabled=true;$("#rl-prog").classList.add("show");const bar=$("#rl-prog").querySelector("i");
  try{
    const stamp=Date.now();
    const cov=await uploadFile(`releases/${myUid}/${stamp}_cover_${rlCover.name.replace(/[^\w.\-]/g,"_")}`,rlCover,p=>bar.style.width=(p*0.5)+"%");
    const aud=await uploadFile(`releases/${myUid}/${stamp}_audio_${rlAudio.name.replace(/[^\w.\-]/g,"_")}`,rlAudio,p=>bar.style.width=(50+p*0.5)+"%");
    await addDoc(collection(db,"releases"),{artistId:myUid,artistName:myName,songName:song,coverURL:cov.url,coverPath:cov.path,audioURL:aud.url,audioPath:aud.path,
      lyrics:$("#rl-lyrics").value.trim(),releaseDate:date,explicit:rlYN.explicit,beatLicenseOwned:rlYN.license,agreementAccepted:true,
      status:"submitted",createdAt:serverTimestamp(),uploaderId:myUid,uploaderName:myName});
    await notifyRole("admin",`${myName} yeni bir release gönderdi: "${song}" (yayın: ${date})`);
    closeModal("m-release");toast("Şarkın yayın için gönderildi ✅");loadReleases();
  }catch(e){msg.classList.add("err");msg.textContent="Hata: "+(e.code||e.message);}
  $("#rl-up").disabled=false;
};
async function loadReleases(){
  const box=$("#release-list");box.innerHTML=`<div class="center-load"><span class="spinner"></span></div>`;
  try{
    let snap;
    if(myRole==="admin"||myRole==="team") snap=await getDocs(query(collection(db,"releases"),orderBy("createdAt","desc")));
    else { const s=await getDocs(query(collection(db,"releases"),where("artistId","==",myUid))); const arr=[];s.forEach(d=>arr.push({id:d.id,data:d.data()}));arr.sort((a,b)=>(b.data.createdAt?.toMillis?.()||0)-(a.data.createdAt?.toMillis?.()||0)); snap={empty:!arr.length,forEach:cb=>arr.forEach(x=>cb({id:x.id,data:()=>x.data}))}; }
    if(snap.empty){box.innerHTML=emptyState("💿","Henüz release yok.");return;}
    box.innerHTML="";
    snap.forEach(s=>{const d=s.data();const canDel=myRole==="admin"||d.artistId===myUid;
      const el=document.createElement("div");el.className="card-item";
      el.innerHTML=`<div style="display:flex;gap:14px;padding:15px 16px;">
        <img src="${esc(d.coverURL)}" style="width:84px;height:84px;border-radius:12px;object-fit:cover;flex-shrink:0;border:1px solid var(--line);">
        <div style="flex:1;min-width:0;">
          <div class="ci-title">${esc(d.songName)}</div>
          <div class="ci-meta"><span>${esc(d.artistName)}</span></div>
          <div class="ci-tags">
            <span class="tag">📅 ${esc(d.releaseDate||"")}</span>
            ${d.explicit?`<span class="tag" style="color:#ff8a8a;border-color:rgba(255,93,93,.35);">EXPLICIT</span>`:`<span class="tag">CLEAN</span>`}
            <span class="tag">${d.beatLicenseOwned?"LİSANS: VAR":"LİSANS: YOK"}</span>
            <span class="tag score">${(d.status||"submitted").toUpperCase()}</span>
          </div>
        </div></div>
        <audio controls preload="none" src="${esc(d.audioURL)}"></audio>
        ${d.lyrics?`<details style="padding:0 16px 14px;"><summary style="cursor:pointer;color:var(--mut);font-size:.74rem;font-family:'Space Mono';">Sözleri gör</summary><div style="font-size:.8rem;color:#bdbdbd;line-height:1.7;white-space:pre-wrap;margin-top:10px;">${esc(d.lyrics)}</div></details>`:""}
        <div class="ci-actions">
          <a class="dl" href="${esc(d.coverURL)}" download>⬇ kapak</a>
          <a class="dl" href="${esc(d.audioURL)}" download>⬇ ses</a>
          ${(myRole==="admin")?`<button class="linklike act-pub" style="color:var(--green);">YAYINLANDI İŞARETLE</button>`:""}
          ${canDel?`<button class="linklike del act-del" style="margin-left:auto;">SİL</button>`:""}
        </div>`;
      if(myRole==="admin")el.querySelector(".act-pub").onclick=async ()=>{await updateDoc(doc(db,"releases",s.id),{status:"published"});await notify(d.artistId,`"${d.songName}" yayınlandı olarak işaretlendi ✅`);toast("Yayınlandı");loadReleases();};
      if(canDel)el.querySelector(".act-del").onclick=async ()=>{ if(!confirm(`"${d.songName}" silinsin mi?`))return; try{if(d.coverPath)await deleteObject(ref(storage,d.coverPath)).catch(()=>{});if(d.audioPath)await deleteObject(ref(storage,d.audioPath)).catch(()=>{});await deleteDoc(doc(db,"releases",s.id));toast("Silindi");loadReleases();}catch(e){toast("Silinemedi");} };
      box.appendChild(el);
    });
  }catch(e){box.innerHTML=emptyState("⚠","Yüklenemedi: "+e.code);}
}

/* ===================================================== ADMIN: USERS ===================================================== */
$("#user-add").onclick=()=>{ ["u-name","u-email","u-pass"].forEach(i=>$("#"+i).value=""); $("#u-role").value="artist"; $("#u-msg").textContent=""; openModal("m-user"); };
$("#u-create").onclick=async ()=>{
  const name=$("#u-name").value.trim(),email=$("#u-email").value.trim(),pass=$("#u-pass").value,role=$("#u-role").value,msg=$("#u-msg");msg.className="msg";
  if(!name||!email||pass.length<6){msg.classList.add("err");msg.textContent="İsim, e-posta ve min 6 haneli şifre gerekli.";return;}
  $("#u-create").disabled=true;
  try{
    const cred=await createUserWithEmailAndPassword(creatorAuth,email,pass); // ikinci app -> kendi oturumun düşmez
    await updateProfile(cred.user,{displayName:name}).catch(()=>{});
    await setDoc(doc(db,"users",cred.user.uid),{displayName:name,email,role,createdAt:serverTimestamp(),createdBy:myUid});
    await signOut(creatorAuth);
    closeModal("m-user");toast("Üye oluşturuldu");loadUsers();
  }catch(e){msg.classList.add("err");msg.textContent=authErr(e.code);}
  $("#u-create").disabled=false;
};
async function loadUsers(){
  const box=$("#users-list");box.innerHTML=`<div class="center-load"><span class="spinner"></span></div>`;
  try{ const snap=await getDocs(collection(db,"users")); box.innerHTML="";
    snap.forEach(s=>{const d=s.data();const uid=s.id;
      const row=document.createElement("div");row.className="admin-row";
      row.innerHTML=`<div class="me-av" style="width:38px;height:38px;border-radius:10px;">${initial(d.displayName)}</div>
        <div class="grow"><div class="t">${esc(d.displayName)} <span class="role-pill ${ROLE_CLASS[d.role]}">${ROLE_LABEL[d.role]||d.role}</span></div>
        <div class="s">${esc(d.email)}</div></div>`;
      const sel=document.createElement("select");sel.className="input";sel.style.width="auto";sel.style.padding="9px 30px 9px 12px";sel.style.fontSize=".75rem";
      ["artist","producer","team","admin"].forEach(r=>{const o=document.createElement("option");o.value=r;o.textContent=ROLE_LABEL[r];if(r===d.role)o.selected=true;sel.appendChild(o);});
      sel.onchange=async ()=>{ await updateDoc(doc(db,"users",uid),{role:sel.value}); toast("Rol güncellendi"); };
      row.appendChild(sel);
      if(uid!==myUid){ const del=document.createElement("button");del.className="btn btn-sm btn-danger";del.style.width="auto";del.textContent="Sil";
        del.onclick=async ()=>{ if(!confirm(`${d.displayName} silinsin mi? (Not: bu sadece yetkisini kaldırır, Auth hesabı Firebase panelinden silinmeli.)`))return; await deleteDoc(doc(db,"users",uid)); toast("Üye kaldırıldı"); loadUsers(); };
        row.appendChild(del); }
      box.appendChild(row);
    });
  }catch(e){box.innerHTML=emptyState("⚠","Yüklenemedi: "+e.code);}
}

/* ===================================================== SETTINGS ===================================================== */
async function reauth(curPass){ const cred=EmailAuthProvider.credential(me.email,curPass); await reauthenticateWithCredential(auth.currentUser,cred); }
$("#se-pass-btn").onclick=async ()=>{
  const m=$("#se-pass-msg");m.className="msg";const cur=$("#se-cur1").value,nw=$("#se-new").value;
  if(!cur||nw.length<6){m.classList.add("err");m.textContent="Mevcut şifre + min 6 haneli yeni şifre.";return;}
  $("#se-pass-btn").disabled=true;
  try{ await reauth(cur); await updatePassword(auth.currentUser,nw); m.classList.add("ok");m.textContent="Şifre güncellendi.";$("#se-cur1").value="";$("#se-new").value=""; }
  catch(e){m.classList.add("err");m.textContent=authErr(e.code);}
  $("#se-pass-btn").disabled=false;
};
$("#se-email-btn").onclick=async ()=>{
  const m=$("#se-email-msg");m.className="msg";const cur=$("#se-cur2").value,em=$("#se-email").value.trim();
  if(!cur||!em){m.classList.add("err");m.textContent="Mevcut şifre + yeni e-posta gerekli.";return;}
  $("#se-email-btn").disabled=true;
  try{ await reauth(cur); await verifyBeforeUpdateEmail(auth.currentUser,em); m.classList.add("ok");m.textContent="Yeni e-postana doğrulama linki gönderildi. Linke tıklayınca değişir."; }
  catch(e){m.classList.add("err");m.textContent=authErr(e.code);}
  $("#se-email-btn").disabled=false;
};

/* ---------- agreement text ---------- */
$("#agreement-body").innerHTML=`
<p><strong>İşbu sözleşme</strong>, eseri yükleyen sanatçı ("Sanatçı") ile müzikHan / etiket sahibi ("Etiket") arasında, Sanatçı'nın platforma yüklediği her eser için geçerlidir. Sanatçı, "okudum, kabul ediyorum" kutusunu işaretleyerek aşağıdaki şartların tamamını kabul eder.</p>
<h4>1. Hak Devri ve Münhasırlık</h4>
<p>Sanatçı, yüklediği esere ilişkin dağıtım, çoğaltma, yayma, dijital platformlarda yayınlama ve ticari kullanım haklarını, eserin tüm yaşam süresi boyunca münhasıran ve geri alınamaz biçimde Etiket'e devreder. Etiket bu hakları üçüncü kişilere devredebilir.</p>
<h4>2. Karar Yetkisi</h4>
<p>Eserin yayınlanıp yayınlanmayacağı, yayın tarihi, kapak, başlık, düzenleme ve ticari koşullar dahil tüm kararlar münhasıran Etiket'e aittir. Sanatçı bu kararlara itiraz hakkından feragat eder.</p>
<h4>3. Gelir Paylaşımı</h4>
<p>Eserden elde edilecek gelirin paylaşımı Etiket tarafından belirlenir ve Etiket tarafından ayrıca bildirilen oranlar geçerlidir. Sanatçı, bu oranlara ve Etiket'in masraf mahsuplarına peşinen muvafakat eder.</p>
<h4>4. Beyan ve Tazmin</h4>
<p>Sanatçı, eserin kendisine ait/özgün olduğunu, üçüncü kişi haklarını ihlal etmediğini beyan eder. Aksi halde doğacak tüm hukuki ve mali sorumluluk Sanatçı'ya aittir; Sanatçı Etiket'i tüm taleplerden beri kılar.</p>
<h4>5. Süre ve Fesih</h4>
<p>Bu sözleşme süresizdir. Etiket dilediği zaman tek taraflı feshedebilir; Sanatçı'nın tek taraflı fesih hakkı yoktur. Fesih halinde dahi devredilen haklar Etiket'te kalır.</p>
<h4>6. Uygulanacak Hukuk</h4>
<p>Uyuşmazlıklarda Türkiye Cumhuriyeti hukuku ve Etiket'in bulunduğu yer mahkemeleri yetkilidir.</p>
<p style="color:var(--dim);font-size:.68rem;margin-top:14px;">Not: Bu metin bir taslaktır. Yürürlükte bağlayıcı bir sözleşme olarak kullanmadan önce bir avukata kontrol ettirmen önerilir.</p>`;
</script>
</body>
</html>
